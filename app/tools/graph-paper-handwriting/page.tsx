import { createToolMetadata, ToolExperiencePage } from "@/components/ToolExperiencePage";

export const metadata = createToolMetadata("graph");

export default function GraphPaperHandwritingPage() {
  return <ToolExperiencePage profile="graph" />;
}
